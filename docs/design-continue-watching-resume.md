# 秒単位レジューム再生の設計（#345）

従来の動画ID履歴はそのまま残し、再生位置だけを別のlocalStorageキーへ保存することで後方互換を保つ。再生位置は有限な0以上の秒数だけを受け入れ、不正値は0秒扱いにする。

動画詳細ページのYouTube IFrame Player APIから15秒ごととpagehide時にcurrentTimeを取得し、ホームの「前回の続きから」リンクを既存のvideoUrlAtTimeへ接続する。動画ID履歴が無い既存利用者は従来どおり動画先頭から再生する。
