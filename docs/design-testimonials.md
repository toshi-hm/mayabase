# 視聴者の声セクション設計（#331）

- 管理者が同意を得たコメントだけを src/data/testimonials.json に手動登録する。初期データは空配列とし、実在しない証言を生成しない。
- JSONは専用パーサで検証する。IDの形式と重複、本文・表示名の長さ、出典URLのHTTPSをビルド時に検証し、HTMLへの値の挿入はAstroのテキストエスケープに任せる。
- トップページのプロフィール直前に、データが1件以上ある場合だけアクセシブルなカード一覧を表示する。出典は任意のHTTPSリンクとして新しいタブで開く。
