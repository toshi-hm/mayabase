# YouTube動画生存確認の設計（#322）

videos.jsonの各動画について、認証不要のYouTube oEmbed GETを定期実行する。2xx以外は取得異常としてIssue通知の対象にし、ネットワーク失敗も「取得失敗」として可視化する。oEmbedの判定は削除・非公開の確定診断ではないため、動画データを自動削除・変更しない。

既存の外部リンク監視と同じく、openのアラートIssueがあればコメントを追加し、全件復旧時に自動クローズする。監視頻度は週次とし、oEmbedへの同時リクエスト数を制限する。
